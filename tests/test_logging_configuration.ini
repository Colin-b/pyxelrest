version: 1
formatters:
  clean:
    format: '%(asctime)s [%(levelname)s] %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: clean
    stream: ext://sys.stdout
root:
  level: DEBUG
  handlers: [console]
